name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: update
      run: |
        apt-get update -y
        apt-get install -y dc coreutils build-essential
        apt-get upgrade -y
    - uses: actions/checkout@v2
    - name: make
      run: make clean all

